image: registry.wndv.co/docker-android-sdk:2.1

stages:
  - distribute

variables:
  GRADLE_USER_HOME: "${CI_PROJECT_DIR}/.gradle"

upload:
  stage: distribute
  script:
    - cd android
    - ./gradlew assembleRelease
    - ./gradlew publishToMavenLocal
    - ./gradlew publish
  only:
    - tags